# Generated by Django 6.1.1 on 2026-09-22 03:32

import django.db.models.deletion
from django.conf import settings
from django.db import migrations, models
from django.utils.crypto import get_random_string

# Duplicated from predictions.models.REFERRAL_CODE_ALPHABET/LENGTH rather
# than imported, so a future change to that constant can't silently change
# what this migration does.
REFERRAL_CODE_ALPHABET = "ABCDEFGHJKMNPQRSTUVWXYZ23456789"
REFERRAL_CODE_LENGTH = 8


def backfill_referral_codes(apps, schema_editor):
    """Every Profile row created before this migration has referral_code
    == "" (the field's temporary default). Give each one a unique code
    before the next operation makes the column unique."""
    Profile = apps.get_model("predictions", "Profile")
    existing = set(
        Profile.objects.exclude(referral_code="").values_list(
            "referral_code", flat=True
        )
    )
    for profile in Profile.objects.filter(referral_code=""):
        code = get_random_string(
            REFERRAL_CODE_LENGTH, allowed_chars=REFERRAL_CODE_ALPHABET
        )
        while code in existing:
            code = get_random_string(
                REFERRAL_CODE_LENGTH, allowed_chars=REFERRAL_CODE_ALPHABET
            )
        existing.add(code)
        profile.referral_code = code
        profile.save(update_fields=["referral_code"])


class Migration(migrations.Migration):

    dependencies = [
        ('predictions', '0014_profile_age'),
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
    ]

    operations = [
        migrations.CreateModel(
            name='ReferralSettings',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('credits_per_referral', models.PositiveIntegerField(default=10, help_text='Credits awarded to the referrer once the referred user submits their first prediction.')),
                ('redemption_threshold', models.PositiveIntegerField(default=100, help_text='Credits required to redeem one voucher.')),
            ],
            options={
                'verbose_name': 'Referral settings',
                'verbose_name_plural': 'Referral settings',
            },
        ),
        migrations.AddField(
            model_name='profile',
            name='credits',
            field=models.IntegerField(default=0),
        ),
        migrations.AddField(
            model_name='profile',
            name='referral_code',
            # Not unique yet: existing rows all default to "" and get a
            # real, unique code from backfill_referral_codes below.
            field=models.CharField(default='', editable=False, max_length=8),
        ),
        migrations.CreateModel(
            name='Referral',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('code_used', models.CharField(max_length=8)),
                ('status', models.CharField(choices=[('pending', 'Pending'), ('credited', 'Credited')], default='pending', max_length=20)),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('credited_at', models.DateTimeField(blank=True, null=True)),
                ('referred_user', models.OneToOneField(on_delete=django.db.models.deletion.CASCADE, related_name='referral_used', to=settings.AUTH_USER_MODEL)),
                ('referrer', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='referrals_made', to=settings.AUTH_USER_MODEL)),
            ],
            options={
                'ordering': ['-created_at'],
            },
        ),
        migrations.CreateModel(
            name='VoucherRedemption',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('credits_spent', models.PositiveIntegerField()),
                ('status', models.CharField(choices=[('pending', 'Pending'), ('fulfilled', 'Fulfilled'), ('rejected', 'Rejected')], default='pending', max_length=20)),
                ('requested_at', models.DateTimeField(auto_now_add=True)),
                ('resolved_at', models.DateTimeField(blank=True, null=True)),
                ('admin_note', models.CharField(blank=True, default='', help_text='Optional note, e.g. the voucher code/reference sent manually.', max_length=255)),
                ('user', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='voucher_redemptions', to=settings.AUTH_USER_MODEL)),
            ],
            options={
                'ordering': ['-requested_at'],
            },
        ),
        migrations.CreateModel(
            name='CreditLedger',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('delta', models.IntegerField()),
                ('reason', models.CharField(choices=[('referral_earned', 'Referral earned'), ('redemption_spent', 'Redemption spent'), ('redemption_refunded', 'Redemption refunded')], max_length=20)),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('user', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='credit_adjustments', to=settings.AUTH_USER_MODEL)),
                ('referral', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='credit_entries', to='predictions.referral')),
                ('redemption', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='credit_entries', to='predictions.voucherredemption')),
            ],
            options={
                'ordering': ['-created_at'],
            },
        ),
        migrations.AddConstraint(
            model_name='referral',
            constraint=models.CheckConstraint(condition=models.Q(('referrer', models.F('referred_user')), _negated=True), name='referral_no_self_referral'),
        ),
        migrations.RunPython(backfill_referral_codes, migrations.RunPython.noop),
        migrations.AlterField(
            model_name='profile',
            name='referral_code',
            field=models.CharField(default='', editable=False, max_length=8, unique=True),
        ),
    ]
